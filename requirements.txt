# ChainMon - 核心依赖（生产环境必需）
flask>=2.3.0          # Web管理后台 + P2P API服务器
flask-cors>=4.0.0     # CORS跨域支持
psutil>=5.9.0         # 系统性能采集（CPU/内存/磁盘/网络）
requests>=2.31.0      # HTTP客户端（P2P节点间通信）
pyyaml>=6.0           # YAML配置文件解析
cryptography>=41.0.0  # AES-256-CBC加密/解密（FRU数据传输加密）

# 以下为可选/测试依赖（当前生产代码未使用）
# paramiko>=3.3.0       # SSH远程执行（测试脚本使用）
# sqlalchemy>=2.0.0     # 数据库ORM（预留）
# apscheduler>=3.10.0   # 定时任务调度（预留）
